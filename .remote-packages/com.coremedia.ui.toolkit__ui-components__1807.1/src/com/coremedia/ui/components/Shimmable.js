Ext.define("com.coremedia.ui.components.Shimmable", function(Shimmable) {/*package com.coremedia.ui.components {
/**
 * Used by IFrameMgr
 *
 * @see IFrameMgr
 * /
public interface Shimmable {

  /**
   * Toggles visibility of the (frontal) transparent shim agent for the frame.  Used primarily for masking the frame during drag operations.
   * @param {Boolean} show Optional True to activate the shim, false to hide the shim agent.
   * /
  function toggleShim(show:Boolean):void;

  function getId():String;
}
}

============================================== Jangaroo part ==============================================*/
    return {};
});
