Ext.define("com.coremedia.cap.common.CapSession", function(CapSession) {/*package com.coremedia.cap.common {
import com.coremedia.cap.user.User;

/**
 * The current user's CMS session used for accessing
 * and modifying the repositories of the CAP system.
 *
 * @see #getConnection()
 * @see #getUser()
 * /
[PublicApi]
public interface CapSession {

  /**
   * Return the CMS remote service this session belongs to.
   * @return the CMS remote service this session belongs to
   * /
  function getConnection():CapConnection;

  /**
   * Return the current user who created this session.
   * @return the current user who created this session
   * /
  function getUser():User;

  /**
   * Close this session and logout the currently logged in user.
   * /
  function close(callback:Function = null):void;
}
}

============================================== Jangaroo part ==============================================*/
    return {};
});
