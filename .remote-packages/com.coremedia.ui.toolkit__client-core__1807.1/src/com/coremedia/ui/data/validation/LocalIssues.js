Ext.define("com.coremedia.ui.data.validation.LocalIssues", function(LocalIssues) {/*package com.coremedia.ui.data.validation {
public interface LocalIssues {

  /**
   * Add a new issue. Does nothing if the issue has already been added.
   *
   * @param severity the severity of this issue
   * @param code a code identifying the type of issue; this should be an identifier that can be localized afterwards; It is discouraged to provide human readable text at this point
   * @param property the property that caused the issue, or {@code null}, if the issue affects the validated object as a whole
   * @param arguments optional argument describing the issue, for example indicating a illegally linked object
   * /
  function addIssue(severity:String, code:String, property:String = null, ... arguments):void;

  /**
   * Add the given issue.
   *
   * @param issue the issue to add
   * /
  function addFromIssue(issue:Issue):void;

  function setPending():void;
}
}

============================================== Jangaroo part ==============================================*/
    return {};
});
