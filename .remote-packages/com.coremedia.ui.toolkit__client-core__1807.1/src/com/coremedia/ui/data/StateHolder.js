Ext.define("com.coremedia.ui.data.StateHolder", function(StateHolder) {/*package com.coremedia.ui.data {

/**
 * <code>StateHolder</code>s provide a <code>ValueExpression</code> containing their current state
 * /
[PublicApi]
public interface StateHolder {

  /**
   * Return a value expression for the state of the component that implements this interface.
   * It allows to retrieve its current state
   * (<code>getStateValueExpression().getValue()</code>) and to observe when the state changes
   * (<code>getStateValueExpression().addValueChangeListener(...)</code>).
   * @return a value expression to compute and observe the state
   * /
  function getStateValueExpression():ValueExpression;
}
}

============================================== Jangaroo part ==============================================*/
    return {};
});
